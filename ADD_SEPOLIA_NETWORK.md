# ✅ THÊM SEPOLIA NETWORK - HƯỚNG DẪN CHI TIẾT

Bạn đang ở cửa sổ **"Quản lý mạng" (Network Management)** - đây là đúng chỗ!

## 🎯 BẠN ĐANG Ở ĐÂU?

Bạn đang thấy:
- ✅ Cửa sổ "Quản lý mạng" (Network Management)
- ✅ Phần "Mạng bổ sung" (Additional Networks)
- ✅ Phần "Test Networks" với toggle "Hiển thị các mạng thử nghiệm" đang BẬT
- ✅ Danh sách test networks, trong đó có **"Sepolia"**

## 📋 CÁC BƯỚC THÊM SEPOLIA

### Bước 1: Kiểm tra toggle "Hiển thị các mạng thử nghiệm"

1. **Tìm toggle switch** ở phần "Test Networks"
2. **Đảm bảo toggle đang BẬT** (màu xanh, sang phải)
   - Nếu chưa bật → Click vào toggle để bật
   - Nếu đã bật → Tiếp tục bước 2

### Bước 2: Tìm Sepolia trong danh sách

1. **Scroll xuống** trong phần "Test Networks"
2. **Tìm "Sepolia"** trong danh sách:
   - Logo: Purple 'S' (chữ S màu tím)
   - Tên: "Sepolia"
   - Có icon 3 chấm dọc (⋮) bên phải

### Bước 3: Thêm Sepolia vào MetaMask

**Có 2 cách:**

#### Cách 1: Click vào Sepolia (Nhanh nhất)

1. **Click trực tiếp vào dòng "Sepolia"**
   - Click vào logo hoặc tên "Sepolia"
   - MetaMask sẽ tự động thêm và chuyển sang Sepolia

2. **Xác nhận:**
   - Có thể có popup xác nhận → Click "Add network" hoặc "Thêm mạng"
   - MetaMask sẽ tự động chuyển sang Sepolia

#### Cách 2: Click icon 3 chấm (Nếu cách 1 không hoạt động)

1. **Click icon 3 chấm dọc (⋮)** bên phải của "Sepolia"
2. **Chọn "Add network"** hoặc "Thêm mạng" từ menu
3. **Xác nhận** trong popup nếu có

### Bước 4: Xác nhận đã thêm Sepolia

Sau khi thêm, bạn sẽ thấy:

1. **Cửa sổ "Quản lý mạng" đóng lại**
2. **MetaMask tự động chuyển sang Sepolia:**
   - Ở góc trên cùng, bạn sẽ thấy "Sepolia" thay vì "Ethereum Mainnet"
   - Balance hiển thị "0 ETH" (chưa có test ETH)

3. **Kiểm tra trong danh sách networks:**
   - Click vào network dropdown (góc trên)
   - Sepolia sẽ xuất hiện trong danh sách
   - Có dấu tick (✓) hoặc highlight nếu đang chọn

---

## 🔍 NẾU KHÔNG THẤY SEPOLIA TRONG DANH SÁCH

### Giải pháp 1: Scroll xuống

1. **Scroll xuống** trong phần "Test Networks"
2. **Sepolia có thể nằm ở dưới** danh sách
3. **Tìm logo purple 'S'** hoặc tên "Sepolia"

### Giải pháp 2: Thêm network thủ công

Nếu không thấy Sepolia trong danh sách:

1. **Click nút "+ Thêm mạng tùy chỉnh"** ở cuối cửa sổ
2. **Nhập thông tin Sepolia:**
   - **Network Name:** `Sepolia`
   - **New RPC URL:** `https://sepolia.infura.io/v3/c7b0ee9f14774684a619e43305849f6f`
   - **Chain ID:** `11155111`
   - **Currency Symbol:** `ETH`
   - **Block Explorer URL:** `https://sepolia.etherscan.io`

3. **Click "Save"** hoặc "Lưu"
4. **MetaMask sẽ tự động chuyển sang Sepolia**

---

## ✅ KIỂM TRA SAU KHI THÊM

### Kiểm tra 1: Network hiện tại

1. **Đóng cửa sổ "Quản lý mạng"** (click X hoặc click ra ngoài)
2. **Xem góc trên cùng của MetaMask:**
   - Phải thấy "Sepolia" (không phải "Ethereum Mainnet")
   - Có thể có icon testnet (ống nghiệm)

### Kiểm tra 2: Balance

1. **Balance hiển thị:**
   - "0 ETH" (chưa có test ETH)
   - Địa chỉ wallet vẫn giữ nguyên

### Kiểm tra 3: Trong danh sách networks

1. **Click vào network dropdown** (góc trên)
2. **Tìm "Sepolia"** trong danh sách
3. **Sepolia sẽ có dấu tick (✓)** hoặc highlight nếu đang chọn

---

## 🎯 TÓM TẮT CÁC BƯỚC

1. ✅ **Đảm bảo toggle "Hiển thị các mạng thử nghiệm" đang BẬT**
2. ✅ **Tìm "Sepolia" trong danh sách Test Networks**
3. ✅ **Click vào "Sepolia"** để thêm và chuyển sang
4. ✅ **Xác nhận:** Ở góc trên phải thấy "Sepolia"

---

## 🆘 TROUBLESHOOTING

### Q: Click vào Sepolia nhưng không có gì xảy ra?

**A:** 
1. Thử click icon 3 chấm (⋮) → "Add network"
2. Hoặc thêm network thủ công (xem Giải pháp 2 ở trên)

### Q: Sepolia đã được thêm nhưng vẫn thấy "Ethereum Mainnet"?

**A:**
1. Click vào network dropdown (góc trên)
2. Tìm và click "Sepolia" trong danh sách
3. Xác nhận chuyển network

### Q: Không thấy Sepolia trong danh sách Test Networks?

**A:**
1. Đảm bảo toggle "Hiển thị các mạng thử nghiệm" đang BẬT
2. Scroll xuống để tìm
3. Hoặc thêm network thủ công (xem Giải pháp 2)

---

## 📋 CHECKLIST

Sau khi thêm Sepolia:

- [ ] ✅ Toggle "Hiển thị các mạng thử nghiệm" đang BẬT
- [ ] ✅ Đã tìm thấy "Sepolia" trong danh sách
- [ ] ✅ Đã click vào Sepolia để thêm
- [ ] ✅ Ở góc trên MetaMask hiển thị "Sepolia"
- [ ] ✅ Balance hiển thị "0 ETH"
- [ ] ✅ Sepolia xuất hiện trong danh sách networks khi click dropdown

---

**Bây giờ: Click vào "Sepolia" trong danh sách Test Networks để thêm và chuyển sang!** 🚀

